# @proveit/verification-contract

Public ProveIT verification record contract package.

This package publishes:

- TypeScript types for `GET /api/v1/verify/{id}` responses.
- A JSON Schema for public verification records.
- Fixture records used by the verifier and private backend contract tests.

```ts
import { verificationSchema } from '@proveit/verification-contract';
```
